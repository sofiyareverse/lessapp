<%= form_for [@train, @carrige] do |form| %>
  <% if carrige.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrige.errors.count, "error") %> prohibited this carrige from being saved:</h2>

      <ul>
      <% carrige.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.radio_button :carrige_type, 'reserved', :checked => true %> 
    <%= label :carrige_type, 'reserved' %>
    <%= form.radio_button :carrige_type, 'compartment' %>
    <%= label :carrige_type, 'compartment' %>
    <%= form.radio_button :carrige_type, 'sitting' %>
    <%= label :carrige_type, 'exclusive' %>
    <%= form.radio_button :carrige_type, 'exclusive' %>
    <%= label :carrige_type, 'sitting' %>
    <%= form.label :up_sits %>
    <%= form.number_field :up_sits %>
    <%= form.label :down_sits %>
    <%= form.number_field :down_sits %>
        <%= form.label :side_down_sits %>
    <%= form.number_field :side_down_sits %>
        <%= form.label :side_up_sits %>
    <%= form.number_field :side_up_sits %>
        <%= form.label :sitting_sits %>
    <%= form.number_field :sitting_sits %>
    <%= form.label :train_id %>
    <%= form.collection_select :train_id, Train.all, :id, :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
