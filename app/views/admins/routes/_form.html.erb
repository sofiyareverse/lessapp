<%= form_with(model: @route, local: true) do |route| %>
  <% if @route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% @route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label :name, 'Name' %>
    <%= route.text_field :name %>
  </div>

  <div class="field">
    <%= route.label :stations %>
    <%= route.collection_select :railway_station_ids, RailwayStation.all, :id, :name, {}, { multiple: true } %>
  </div>

  <div class="actions">
    <%= route.submit %>
  </div>
<% end %>
